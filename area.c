#include<stdio.h>
int main()
{
    int l,b,area,perimeter;
    printf("enter the value of l,b");
    scanf("%d %d",&l,&b);
    area=l*b;
    perimeter=2(l+b);
    printf("%d %d",area,perimeter);
}